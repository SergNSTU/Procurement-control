<Window x:Class="ProcurementControl.Views.DealCompletionReceiptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Поступление комплектации" Width="560" Height="400"
        WindowStartupLocation="CenterOwner" Background="{StaticResource CanvasBrush}">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Сохранить" Style="{StaticResource PrimaryButtonStyle}" Width="110" Height="30" Margin="0,0,8,0" Click="SaveClick"/>
            <Button Content="Отмена" Style="{StaticResource SecondaryButtonStyle}" Width="90" Height="30" IsCancel="True" Click="CancelClick"/>
        </StackPanel>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <TextBlock Text="Укажите фактическую дату поступления для каждого поставщика." Foreground="{StaticResource MutedBrush}" Margin="0,0,0,10"/>
            <DataGrid Grid.Row="1" ItemsSource="{Binding Rows}" AutoGenerateColumns="False" CanUserAddRows="False"
                      CanUserDeleteRows="False" HeadersVisibility="Column" SelectionMode="Single" SelectionUnit="CellOrRowHeader">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Поставщик" Binding="{Binding Supplier}" Width="*" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Факт поступления" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate><TextBlock Text="{Binding ReceiptDate, StringFormat={}{0:dd.MM.yyyy}}" Padding="8,0"/></DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate><DatePicker SelectedDate="{Binding ReceiptDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedDateFormat="Short"/></DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
